# 需求评审 Prompt

> 此 Prompt 用于 AI 对需求文档和技术方案进行专业评审

---

## ⛔ 禁止 AI 擅自做决定（强制按流程规范执行）

> **⚠️ 执行此 Prompt 前，必须完整阅读本文档！禁止凭记忆或推断执行！**

| 禁止项 | 说明 | 正确做法 |
|--------|------|---------|
| ⛔ 禁止假设已读取 | 不假设前置步骤已完成，必须实际执行每步读取操作 | 每个步骤按规范重新读取，不假设前置状态 |
| ⛔ 禁止简化 Todo | Todo 清单必须完整，不可省略强制检查项（⭐标记项） | 按规范创建完整 Todo 清单（13项），逐项标记状态 |
| ⛔ 禁止跳过步骤 | 强制项（⭐⭐⭐）不可跳过，必须逐项执行并标记状态 | 强制项必须执行，AI 无权判断跳过 |
| ⛔ 禁止自行判断 | AI 不可自行判断「不重要」「已完成」「无需执行」 | 按流程规范执行，不做任何擅自决策 |
| ⛔ 禁止假设状态 | 标记「已完成」前必须实际执行该操作，不可凭记忆标记 | 每项必须实际执行后才能标记 completed |
| ⛔ 禁止跳过前置检查 | 前置检查（0-1/0-2/0-3）不可跳过 | 必须先执行前置检查，再执行评审流程 |

**典型违规场景**：

| 违规行为 | 正确做法 |
|---------|---------|
| 假设"步骤②已读过需求文档，无需再读" | 每个步骤按规范重新读取，不假设前置状态 |
| 简化 Todo 为 5 项而非 13 项 | 按规范创建完整 Todo 清单，逐项标记状态 |
| 标记"已完成"但实际未执行读取操作 | 每项必须实际执行后才能标记 completed |
| 跳过"扫描图片目录"认为不重要 | 强制项必须执行，AI 无权判断跳过 |

---

## ⚠️ 前置检查流程（最高优先级）⭐⭐⭐

> **AI 执行需求评审前，必须先完成前置检查！跳过前置检查属于流程错误！**
> 
> 💡 **项目根目录已缓存时直接使用，无需重复定位和检查初始化。**

### 前置检查步骤

| 序号 | 检查项 | 强制 | AI 行为 |
|------|--------|------|---------|
| 0-1 | 项目根目录（如未缓存则定位） | ⭐⭐⭐ | 已缓存→直接使用；未缓存→向上查找 `.skill/`（最多3层） |
| 0-2 | 项目初始化检查（仅首次定位时执行） | ⭐⭐⭐ | 未初始化→创建知识库；已初始化→跳过 |
| 0-3 | 整理版 MD 检查 | ⭐⭐⭐ | 检查是否存在 `*整理版*.md`（来源可以是本地目录整理或 Confluence 页面提取），**不存在则提示先执行①文档整理/Confluence 提取** |

### 检查流程

```
用户触发需求评审
    │
    ↓
检查 0-1：项目根目录
    │
    ├─ 已缓存 → 直接使用
    ├─ 未缓存 → 定位（向上查找 .skill/，最多3层）→ 缓存
    │   └─ 定位失败 → 询问用户「项目根目录在哪里？」
    │
    ↓
检查 0-2：项目初始化（仅首次定位时执行）
    │
    ├─ 已初始化 → 继续
    ├─ 未初始化 → 执行初始化流程（创建目录+询问配置）
    │
    ↓
检查 0-3：整理版 MD 是否存在
    │
    ├─ 存在 → 读取整理版 MD，继续评审
    │        （来源不限：本地目录整理 或 Confluence 页面提取 均可）
    ├─ 不存在 → ⚠️ 提示用户先执行①文档整理，或提供 Confluence URL 提取页面
    │
    ↓
创建 Todo 清单 → 执行评审流程
```

### ❌ 禁止行为

| 禁止项 | 说明 |
|--------|------|
| ❌ 跳过前置检查 | 不执行前置检查直接开始评审属于流程错误 |
| ❌ 每个步骤重复定位项目根目录 | 应使用缓存的项目根目录 |
| ❌ 每个步骤重复检查初始化 | 仅首次定位时检查 |
| ❌ 整理版 MD 不存在时强行评审 | 必须先完成文档整理（本地目录）或 Confluence 页面提取 |

---

## ⚠️ 执行前必须创建 Todo 清单 ⭐

> **AI 执行此流程时，必须先调用 `todo_write` 创建以下 todo 清单，逐项完成并标记状态！**
>
> ⚠️ **注意：Todo 清单前 3 项为前置检查步骤，必须先完成！**

**步骤②子 Todo 清单（13项强制步骤）**：

| 序号 | 步骤 | 强制 | 输出/验证 |
|------|------|------|----------|
| 0-1 | 项目根目录（如未缓存则定位） | ⭐⭐⭐ | 已缓存→直接使用；未缓存→定位并缓存 |
| 0-2 | 项目初始化检查（仅首次定位时执行） | ⭐⭐⭐ | 未初始化→创建；已初始化→跳过 |
| 0-3 | 整理版 MD 检查 | ⭐⭐⭐ | 检查 `*整理版*.md`，不存在则提示先执行① |
| 1 | 读取 JSON 格式规范（本文档「JSON 格式规范」章节） | ⭐ 强制 | 理解 doc_title、categories[].items[] 等关键字段 |
| 2 | 读取精华库 {kb_path}/patterns/精华库/common_root_causes.md + common_omissions.md | ⭐ 默认执行 | 轻量路径：AI 错题本（≤300 Token） |
| 3 | 读取知识库历史文档 {kb_path}/requirements/*.md | 可选 | **完整路径**：用户说"结合历史"等关键词时执行 |
| 4 | 读取需求文档 MD | ⭐ 强制 | 主评审文档 |
| 5 | 搜索技术方案 MD（搜索 TR_*.md） | 推荐 | 关联技术方案 |
| 6 | 扫描图片目录 images/ | ⭐ 强制 | 记录到 document_status.images |
| 7 | 检查附件目录 attachments/ | ⭐ 强制 | 记录到 document_status.attachments |
| 8 | 多维度评审（按本文档定义的评审维度） | ⭐ 强制 | 逐维度检查 |
| 9 | 需求-技术方案对比分析 | ⭐ 强制 | 记录到 comparison.diff_items |
| 10 | 技术风险识别 | ⭐ 强制 | 记录到 tech_risks |
| 11 | 按格式规范生成 JSON | ⭐ 强制 | 逐字段对照验证 |
| 12 | 调用脚本生成评审报告 MD（generate_review_report.py） | ⭐ 强制 | 包含知识库读取状态、图片解析状态章节 |

**AI 执行流程**：
1. 收到步骤②指令 → 立即调用 `todo_write` 创建 13 项子清单（含前置检查）
2. **先执行 0-1、0-2、0-3 前置检查步骤**
3. 前置检查完成后 → 逐项执行 1-12 评审步骤
4. 每完成一项立即标记 `completed`
5. 强制项跳过 → 用户会看到 todo 状态未完成 → 立即发现问题

---

## 任务目标

对需求文档和技术方案文档（MD 格式）进行专业评审，从多个维度发现潜在问题，生成结构化评审报告，用于需求评审会议、技术方案评审会议或文档完善。

---

## 输入

| 输入项 | 来源 | 必选 |
|--------|------|------|
| 需求文档 MD | 用户提供 | ⭐ 必选 |
| 技术方案 MD | 自动搜索 | 推荐 |
| 图片目录 | 同目录 `images/` | 推荐 |
| 知识库历史文档 | `{kb_path}/requirements/` | 推荐 |
| 精华库 | `{kb_path}/patterns/精华库/` | 推荐 |

---

## 输出

> ⚠️ **输出路径规则**：所有输出文件统一存放到用户故事工作目录（`D:\效贷-产品需求\{用户故事编号}-{用户故事名称}`），由步骤①确认并缓存为 `{user_story_dir}`。如会话中已缓存用户故事目录路径，直接使用；如未缓存，需先向测试人员确认用户故事信息（编号+名称）并定位/创建工作目录。

| 输出项 | 格式 | 命名 | 输出路径 |
|--------|------|------|---------|
| 评审报告 | .md | `2.{需求名}_评审报告.md` | `{user_story_dir}/` |
| 评审数据 | .json | `2.{需求名}_评审数据.json` | `{user_story_dir}/` |

---

## JSON 格式规范 ⭐

> ⚠️ **JSON 格式必须严格遵循以下规范，与 generate_review_report.py 脚本一致！**

**AI 评审结果必须按以下 JSON 格式组织：**

```json
{
  "doc_title": "{文档名称}",
  "doc_type": "技术方案",
  "review_date": "2026-05-06",
  "reviewer": "AI 评审",
  "summary": {
    "total_issues": 15,
    "critical": 3,
    "major": 7,
    "minor": 5
  },
  "document_status": {
    "knowledge_base": {
      "exists": true,
      "message": "知识库已读取",
      "history_items": [
        {
          "type": "历史需求",
          "name": "额度测算.md",
          "status": "✅ 已读取",
          "content": "征信查询流程、状态定义"
        }
      ]
    },
    "related_documents": [
      {
        "type": "需求文档",
        "filename": "额度测算.md",
        "status": "✅ 已读取",
        "remark": "主评审文档"
      },
      {
        "type": "技术方案",
        "filename": "TR_额度测算.md",
        "status": "✅ 已读取",
        "remark": "关联技术方案"
      }
    ],
    "images": [
      {
        "name": "业务流程图.png",
        "location": "需求文档 2.1 方案概述",
        "status": "✅ 已解析",
        "type": "流程图",
        "info": "包含额度测算完整流程，5个关键节点",
        "reason": ""
      },
      {
        "name": "界面原型图.png",
        "location": "需求文档 3.2 页面设计",
        "status": "❌ 未解析",
        "type": "界面原型图",
        "info": "",
        "reason": "当前模型不支持图片输入，无法解析界面原型。建议：切换到支持图片的模型重新评审，或手动描述图片内容（页面元素、交互操作、功能覆盖）。"
      }
    ],
    "attachments": [
      {
        "name": "接口文档.pdf",
        "location": "需求文档附件",
        "status": "✅ 已转换",
        "type": "接口文档",
        "content": "凯旋征信接口参数定义"
      }
    ]
  },
  "categories": [
    {
      "name": "需求疑问",
      "items": [
        {
          "title": "参数取值含义未说明",
          "location": "1.2 功能描述",
          "description": "参数取值含义未说明，影响开发实现",
          "suggestion": "补充参数各取值的详细说明",
          "severity": "critical",
          "status": "待解决"
        }
      ]
    },
    {
      "name": "模糊点",
      "items": [
        {
          "title": "时间范围定义模糊",
          "location": "2.3 有效期说明",
          "description": "'当天有效'的具体时间范围未明确",
          "suggestion": "明确定义为签署当天24:00:00截止",
          "severity": "major",
          "status": "待解决"
        }
      ]
    },
    {
      "name": "完整性检查",
      "items": [
        {
          "title": "缺少埋点需求",
          "location": "文档整体",
          "description": "关键操作缺少埋点需求说明",
          "suggestion": "补充埋点事件和属性定义",
          "severity": "major",
          "status": "缺失"
        }
      ]
    },
    {
      "name": "数据库设计",
      "items": [
        {
          "title": "金额字段类型不当",
          "location": "t_apply_credit_quota表",
          "description": "金额字段使用 INT 类型，精度不足",
          "suggestion": "金额字段应使用 DECIMAL(18,2) 类型",
          "severity": "critical",
          "status": "缺失"
        }
      ]
    },
    {
      "name": "接口设计",
      "items": [
        {
          "title": "接口幂等设计未明确",
          "location": "接口设计整体",
          "description": "重复调用同一接口的处理逻辑未说明",
          "suggestion": "补充接口幂等设计说明",
          "severity": "major",
          "status": "缺失"
        }
      ]
    },
    {
      "name": "技术风险",
      "items": [
        {
          "title": "第三方系统SLA未评估",
          "location": "第三方依赖",
          "description": "外部系统稳定性依赖风险未评估",
          "suggestion": "定义接口超时阈值、重试策略、降级方案",
          "severity": "critical",
          "status": "缺失"
        }
      ]
    }
  ],
  "comparison": {
    "diff_items": [
      {
        "type": "需求-技术方案不一致",
        "req_desc": "需求文档状态翻转'15天后'",
        "tech_desc": "技术方案Apollo配置'expireDays=14'",
        "impact": "过期天数定义不一致，影响业务逻辑"
      }
    ]
  },
  "historical_comparison": {
    "items": [
      {
        "dimension": "状态定义",
        "history_issue": "上次评审指出状态流转不完整",
        "resolved": "✅ 已解决",
        "status": "已补充状态流转图"
      }
    ],
    "description": "基于知识库历史评审报告对比"
  },
  "tech_risks": [
    {
      "risk": "依赖外部系统，任一异常影响核心功能",
      "level": "🔴 严重",
      "suggestion": "定义SLA、超时阈值、重试策略、降级方案"
    },
    {
      "risk": "异步流程并发场景可能导致数据不一致",
      "level": "🟡 重要",
      "suggestion": "设计分布式锁机制，明确事务边界"
    }
  ],
  "completeness_check": [
    {
      "name": "版本记录",
      "status": "有",
      "description": "V1.0、V1.1"
    },
    {
      "name": "需求背景",
      "status": "有",
      "description": "已说明解决什么问题"
    },
    {
      "name": "接口定义",
      "status": "缺失",
      "description": "缺少内部接口定义"
    }
  ]
}
```

---

## 字段说明（必读）⭐

### 顶级字段

| 字段路径 | 含义 | 必填 | 说明 |
|---------|------|------|------|
| `doc_title` | 文档名称 | ✅ | 格式：`{需求名称}` |
| `doc_type` | 文档类型 | ✅ | `需求文档`/`技术方案`/`混合文档` |
| `review_date` | 评审日期 | ✅ | YYYY-MM-DD 格式 |
| `reviewer` | 评审人 | ✅ | 固定值：`AI 评审` |
| `summary` | 评审概览 | ✅ | 问题统计 |
| `document_status` | 文档读取状态 | ✅ | **8 个必选状态标记** |
| `categories` | 问题分类数组 | ✅ | **顶层数组，所有问题放在此处** |
| `comparison` | 需求-技术方案对比 | ⚠️ | 有技术方案时必填 |
| `historical_comparison` | 历史对比分析 | ⚠️ | 有历史记录时必填 |
| `tech_risks` | 技术风险提示 | ✅ | 数组格式 |
| `completeness_check` | 完整性检查 | ✅ | 数组格式 |

### document_status 字段（8 个必选状态标记）

| 字段路径 | 类型 | 必填 | 说明 |
|---------|------|------|------|
| `document_status.knowledge_base` | 对象 | ✅ | 知识库读取状态 |
| `knowledge_base.exists` | boolean | ✅ | **知识库目录是否存在**（true=目录存在，false=目录不存在/未初始化） |
| `knowledge_base.message` | string | ✅ | 读取结果描述（如："暂无历史文档"） |
| `knowledge_base.history_items` | 数组 | ⚠️ | 历史文档列表（有数据则填写，无数据则空数组） |
| `document_status.related_documents` | **数组** | ✅ | **⚠️ 必须是数组，不可是字符串！** |
| `related_documents[].type` | string | ✅ | 文档类型 |
| `related_documents[].filename` | string | ✅ | 文件名 |
| `related_documents[].status` | string | ✅ | 读取状态 |
| `related_documents[].remark` | string | ✅ | 备注 |
| `document_status.images` | **数组** | ✅ | **⚠️ 必须是数组，不可是字符串！** |
| `images[].name` | string | ✅ | 图片名称 |
| `images[].location` | string | ✅ | 引用位置 |
| `images[].status` | string | ✅ | 解析状态（✅ 已解析/❌ 未解析） |
| `images[].type` | string | ✅ | 内容类型（流程图/界面原型图/状态机图等） |
| `images[].info` | string | ⚠️ | 关键信息（已解析时填写） |
| `images[].reason` | string | ⚠️ | **未解析原因**（未解析时必填，说明原因和解决建议） |
| `document_status.attachments` | 数组 | ⚠️ | 附件列表（有则填写） |

> ⚠️ **知识库状态语义说明**：
> - `exists=true` + `history_items=[]` → 知识库已初始化，但暂无历史数据
> - `exists=true` + `history_items有数据` → 正常读取历史文档
> - `exists=false` → 知识库目录不存在（项目未初始化）

> ⚠️ **图片未解析原因规范**：
> - 必须说明具体原因（如：当前模型不支持图片输入）
> - 必须提供解决建议（如：切换模型或手动描述内容）
> - 必须标注影响范围（如：流程图、界面原型关键信息遗漏）

### categories 字段（问题分类）

| 字段路径 | 类型 | 必填 | 说明 |
|---------|------|------|------|
| `categories` | **数组** | ✅ | **⚠️ 顶层数组，不嵌套在 requirement_review/technical_review 下！** |
| `categories[].name` | string | ✅ | 分类名称（如：需求疑问、数据库设计） |
| `categories[].items` | 数组 | ✅ | 问题项数组 |
| `items[].title` | string | ✅ | 问题标题（简短概括） |
| `items[].location` | string | ✅ | 问题位置 |
| `items[].description` | string | ✅ | 问题描述 |
| `items[].suggestion` | string | ✅ | 改进建议 |
| `items[].severity` | string | ✅ | `critical`/`major`/`minor` |
| `items[].status` | string | ✅ | 处理状态（待解决/缺失/待确认等） |

### comparison 字段（需求-技术方案对比）

| 字段路径 | 类型 | 必填 | 说明 |
|---------|------|------|------|
| `comparison.diff_items` | 数组 | ⚠️ | 差异项列表 |
| `diff_items[].type` | string | ✅ | 差异类型 |
| `diff_items[].req_desc` | string | ✅ | 需求文档描述 |
| `diff_items[].tech_desc` | string | ✅ | 技术方案描述 |
| `diff_items[].impact` | string | ✅ | 影响说明 |

### tech_risks 字段（技术风险）

| 字段路径 | 类型 | 必填 | 说明 |
|---------|------|------|------|
| `tech_risks` | 数组 | ✅ | 技术风险列表 |
| `tech_risks[].risk` | string | ✅ | 风险描述 |
| `tech_risks[].level` | string | ✅ | 风险等级（🔴 严重/🟡 重要/🟢 一般） |
| `tech_risks[].suggestion` | string | ✅ | 建议措施 |

---

## ⚠️ 常见错误警告

| 错误类型 | 错误示例 | 正确示例 |
|---------|---------|---------|
| **related_documents 类型错误** | `"related_documents": "已读取"`（字符串） | `"related_documents": [{"type": "需求文档", ...}]`（数组） |
| **images 类型错误** | `"images": {"status": "未解析"}`（对象） | `"images": [{"name": "xxx.png", ...}]`（数组） |
| **categories 嵌套错误** | `"requirement_review": {"categories": [...]}`（嵌套） | `"categories": [{"name": "需求疑问", ...}]`（顶层） |
| **items 缺少 title** | 只有 `location/description` | 必须有 `title` 字段（简短概括） |
| **items 缺少 status** | 没有 `status` 字段 | 必须有 `status` 字段（处理状态） |

---

## 执行步骤

### 前置步骤 1：知识库读取（分层策略）

> ⚠️ **默认走轻量路径，用户明确要求时才走完整路径！**

**知识库路径分层**：

| 路径 | 触发条件 | 执行内容 |
|------|---------|---------|
| **轻量路径（默认）** | 无特殊指令 | ✅ 读取精华库（≤300 Token）<br>❌ 跳过历史文档搜索 |
| **完整路径** | 用户说以下任意表述 | ✅ 读取精华库<br>✅ 搜索并读取历史文档<br>✅ 历史对比分析 |

**完整路径触发关键词**：

| 类别 | 触发表述 |
|------|---------|
| 直接指令 | "结合历史"、"参考知识库"、"查一下知识库" |
| 历史对比 | "结合历史需求分析"、"对比历史"、"看看之前有没有类似的"、"历史有没有类似问题" |
| 经验复用 | "参考历史经验"、"过往经验"、"历史评审"、"之前的需求"、"之前做过的" |
| 同类搜索 | "同类需求"、"类似需求"、"有没有做过类似的"、"历史用例" |
| 复用历史 | "复用历史"、"历史数据"、"历史文档" |

---

#### 轻量路径（默认执行）

**前置 1.1 检查知识库目录**

确认 `{kb_path}/` 目录存在。

**前置 1.2 读取精华库** ⭐

> ⚠️ **精华库路径**：`{kb_path}/patterns/精华库/`
>
> **读取文件**：common_root_causes.md + common_omissions.md
>
> **固定 Top 10 上限，Token 消耗可控！**

读取精华库文件，**学习以下内容**：

| 精华库内容 | 学习目标 |
|---------|---------|
| 高频根因 Top 10 | 了解历史评审遗漏根因，本次避免 |
| 高频遗漏场景 Top 10 | 了解常见遗漏场景类型，本次重点检查 |

**⚠️ 性能控制**：
- 精华库固定 Top 10 上限
- Token 消耗 ≤ 300

**学习重点**：
- 高频根因 → 本次避免共性错误
- 高频遗漏场景 → 本次重点检查

**前置 1.3 呈现读取状态**

```markdown
## 📚 知识库读取状态（轻量路径）

| 知识库内容 | 文档名称 | 读取状态 | 提取内容 |
|-----------|---------|---------|---------|
| 精华库 | patterns/精华库/common_root_causes.md | ✅ 已读取 | 高频根因：隐性需求识别不足 |
| 精华库 | patterns/精华库/common_omissions.md | ✅ 已读取 | 高频遗漏：边界条件未覆盖 |

或

| 知识库内容 | 文档名称 | 读取状态 | 提取内容 |
|-----------|---------|---------|---------|
| 精华库 | patterns/精华库/common_root_causes.md | ⚠️ 初始化状态 | 知识库刚初始化，无历史精华记录 |
```

> 💡 **轻量路径完成，跳过历史文档搜索，直接进入前置步骤 2。**

---

#### 完整路径（用户触发"结合历史"等关键词时执行）

**前置 1.4 读取索引文件**

读取 `{kb_path}/INDEX.md`（如有）。

**前置 1.5 搜索相关历史文档**

根据以下维度搜索：
- **业务领域匹配**：当前需求属于「贸易融资」→ 搜索 requirements/ 下贸易融资类文档
- **功能相似匹配**：当前需求涉及「征信查询」→ 搜索包含征信功能的历史文档
- **关键词匹配**：当前需求关键词 → 搜索 INDEX.md 中的关键词

**前置 1.6 读取历史文档**

读取匹配到的历史文档（最多 3 份），提取：
- 历史需求中的业务规则
- 历史评审报告中的问题项（同类问题是否重复出现）
- 历史技术方案中的接口定义、状态定义

**前置 1.7 读取历史问题解决状态** ⭐

> ⚠️ **已解决的问题是最佳实践参考！**

从历史评审报告中提取问题解决状态：

| 问题状态 | 处理方式 |
|---------|---------|
| ✅ 已解决 | 提取解决方案，用于同类问题参考 |
| ⏳ 处理中 | 标记为待跟踪，避免重复提出 |
| ❌ 未解决 | 继续追问，确认当前状态 |

**同步规则**：
- 同类问题已解决 → 引用解决方案，不再重复提出
- 同类问题处理中 → 说明当前状态，询问最新进展
- 新发现的同类问题 → 对比历史问题，说明差异点

**前置 1.8 呈现读取状态（完整路径）**

```markdown
## 📚 知识库读取状态（完整路径）

| 知识库内容 | 文档名称 | 读取状态 | 提取内容 |
|-----------|---------|---------|---------|
| 历史需求 | 额度测算.md | ✅ 已读取 | 征信查询流程、状态定义 |
| 历史评审报告 | 额度测算_评审报告.md | ✅ 已读取 | 征信接口超时未定义（已解决） |
| 问题解决状态 | - | ✅ 已提取 | 接口超时问题→方案：增加熔断机制 |
| 精华库 | patterns/精华库/common_root_causes.md | ✅ 已读取 | 高频根因：隐性需求识别不足 |
| 业务规则库 | patterns/需求分析规则库.md | ✅ 已读取 | 隐性需求识别规则 |

或

| 历史需求 | - | ❌ 未找到相关文档 | 知识库中暂无贸易融资类需求 |
| 精华库 | patterns/精华库/common_root_causes.md | ⚠️ 初始化状态 | 知识库刚初始化，无历史精华记录 |
```

---

### 前置步骤 2：技术方案评审特殊流程

> ⚠️ **技术方案评审必须关联需求文档和需求评审报告！**

当识别文档类型为「技术方案」时，必须执行：

**前置 2.1 搜索关联需求文档**

搜索同目录及上级目录的 `.md` 文件。

**前置 2.2 搜索需求评审报告**

搜索 `2.*_评审报告.md`（步骤②产出，文件名前缀 "2."）。

**前置 2.3 呈现文档读取状态**

```markdown
## 📄 关联文档读取状态

| 文档类型 | 文件名 | 读取状态 | 备注 |
|---------|--------|---------|------|
| 技术方案 | TR_申贷进件流_额度测算.md | ✅ 已读取 | 主评审文档 |
| 需求文档 | 额度测算.md | ✅ 已读取 | 关联需求文档 |
| 需求评审报告 | 额度测算_评审报告.md | ✅ 已读取 | 包含 25 个问题项 |
```

**前置 2.4 需求-技术方案对比分析**

| 对比维度 | 检查内容 | 示例 |
|---------|---------|------|
| **需求覆盖性** | 技术方案是否覆盖所有功能点 | 需求提出"重新测额"，技术方案是否有对应设计？ |
| **问题解决性** | 技术方案是否解决需求评审问题 | 需求评审指出"状态流转不完整"，技术方案是否补充？ |
| **待确认项响应** | 技术方案是否响应待确认项 | 需求中"Apollo配置项待确认"，技术方案是否已明确？ |

---

### 步骤 1：扫描图片和附件 ⭐

> ⚠️ **图片解析是评审的必要前提，不可跳过！**

**1.1 扫描图片目录**

检查 `images/` 目录。

**1.2 提取图片引用**

从 MD 内容提取所有 `![](images/xxx.png)`。

**1.3 解析图片内容**

| 图片类型 | 评审要点 |
|---------|---------|
| 业务流程图 | 流程完整性、分支覆盖、循环/死锁检查 |
| 界面原型图 | 页面元素、交互操作、功能覆盖 |
| 状态机图 | 状态闭环、非法流转检查 |
| 架构图 | 分层合理性、模块边界清晰 |
| 时序图 | 调用顺序合理、异常处理 |
| 数据模型图 | 关系正确、字段完整 |

**如模型不支持图片**：

明确提示用户并列出未解析图片清单，建议更换模型或手动描述。

---

**1.4 检查附件格式** ⭐

> ⚠️ **附件内容是需求的一部分，未转换则无法评审！**

检查 `attachments/` 目录中的附件文件格式：

| 附件格式 | 处理方式 | 说明 |
|---------|---------|------|
| `.md` | ✅ 直接读取 | 无需转换 |
| `.doc/.docx/.pdf` | ⚠️ 必须转换为 MD | 文档类附件必须转换后评审 |
| `.ppt/.pptx` | ⚠️ 必须转换为 MD | 演示类附件必须转换后评审 |
| `.xlsx/.xls` | 📊 评估转换 | 数据类附件可选择性转换 |
| `.txt/.json/.xml` | ✅ 直接读取 | 无需转换 |
| `.png/.jpg/.gif` | 🖼️ 图片处理 | 按图片评审要点处理 |

---

**1.5 附件转换流程** ⭐

如附件未转换为 MD，必须执行：

```
1. 检测附件目录
   - 扫描 attachments/ 目录
   - 列出所有附件文件

2. 识别待转换附件
   - 筛选 .doc/.docx/.pdf/.ppt/.pptx 格式
   - 输出待转换清单

3. 执行转换
   python {技能目录}/scripts/convert_to_md.py <附件文件>

4. 读取转换后的 MD 内容
   - 将附件内容纳入评审范围
   - 提取附件中的关键信息（业务规则、接口定义等）

5. 呈现附件读取状态
   | 附件名称 | 原格式 | 转换状态 | 读取状态 | 提取内容 |
   |---------|--------|---------|---------|---------|
   | 补充说明.doc | .doc | ✅ 已转换 | ✅ 已读取 | 补充校验规则 |
   | 数据字典.xlsx | .xlsx | 📊 数据附件 | ✅ 已读取 | 字段定义 |
```

**转换优先级**：

| 优先级 | 附件类型 | 说明 |
|--------|---------|------|
| 高 | 补充需求说明文档 | 可能包含关键业务规则 |
| 高 | 接口定义文档 | 评审接口设计的关键依据 |
| 中 | 数据字典/字段说明 | 辅助理解数据结构 |
| 低 | 参考模板/示例 | 可选择性转换 |

---

### 步骤 2：文档类型识别

| 类型 | 判断依据 | 评审侧重 |
|------|---------|---------|
| **需求文档** | 包含「需求背景」「功能描述」「用户故事」 | 需求评审维度（1-6） |
| **技术方案** | 包含「接口设计」「表设计」「技术选型」 | 技术方案评审维度（7-16） |
| **混合文档** | 同时包含两类内容 | 全维度评审 |

---

### 步骤 3：逐维度评审 ⭐

> ⚠️ **评审聚焦原则**：聚焦需求本身的缺陷，不评审运维细节、实现细节！

---

#### 一、需求评审维度（必审）

| 维度 | 检查项 | 示例 |
|------|--------|------|
| **1. 需求疑问** | 业务逻辑不清、边界缺失、状态流转缺失、异常处理缺失 | "当天有效"是几点到几点？ |
| **2. 模糊点** | 用词模糊、量化缺失、条件不完整、时间定义模糊 | "快速返回结果"—多快算快速？ |
| **3. 历史冲突** | 流程冲突、状态冲突、数据冲突、权限冲突 | 新流程与现有流程矛盾 |
| **4. 完整性检查** | 版本记录、需求背景、核心功能覆盖 | 是否有版本变更记录 |
| **5. 可测试性** | 验证标准明确、测试数据可得、边界值可测 | "提升用户体验"无法量化测试 |
| **6. 需求风险** | 第三方依赖风险（需定义超时/降级）、合规风险 | 依赖外部系统稳定性 |

---

#### 二、技术方案评审维度（精简）

> ⚠️ **只评审影响需求实现的关键技术问题，不评审运维细节！**

| 维度 | 检查项 | 示例 | 评审深度 |
|------|--------|------|---------|
| **7. 核心流程** | 流程完整、状态流转、关键异常分支 | 是否遗漏关键异常分支 | **必审** |
| **8. 数据库设计** | 关键字段类型、必要字段缺失 | 金额字段是否用 DECIMAL | **必审** |
| **9. 接口设计** | 关键参数缺失、幂等设计 | 是否缺少关键返回字段 | **必审** |
| **10. 需求-技术方案一致性** | 技术方案是否覆盖需求功能、状态定义一致 | 需求15天 vs 技术14天 | **必审** |
| **11. 安全合规提示** | 敏感数据加密提示、权限控制提示 | 只提示"需加密"，不指定算法 | **轻量提示** |
| **12. 第三方依赖风险** | 外部系统超时、降级、重试机制 | 需定义超时阈值 | **必审** |

---

#### 三、不评审的内容 ⭐ 明确约束

| 类别 | 不评审内容 | 原因 |
|------|-----------|------|
| **运维细节** | 灰度发布方案、故障恢复方案、日志格式规范、日志存储策略 | 这是运维设计阶段的内容 |
| **监控细节** | 告警渠道、告警去重、告警恢复、监控指标定义 | 需求只需说"异常需告警"，具体配置是运维决定 |
| **实现细节** | 具体加密算法（AES-256）、具体缓存策略、具体日志级别 | 需求只需说"需加密"，具体方案是技术决定 |
| **扩展细节** | 多资方扩展架构、插件化设计、水平扩展方案 | 这是架构设计阶段的内容，除非需求明确要求 |
| **性能细节** | 具体轮询频率、具体缓存时效、批量处理数量上限 | 需求只需说"需考虑性能"，具体参数是技术决定 |
| **技术选型** | 具体框架版本、具体组件选型 | 这是技术团队决定，除非涉及新技术风险 |

---

#### 四、评审问题分级标准

| 级别 | 定义 | 适用场景 |
|------|------|---------|
| 🔴 **严重** | 阻塞需求实现、业务逻辑错误、状态流转缺失 | 必须在评审会议讨论 |
| 🟡 **重要** | 影响测试验证、需求与技术方案不一致、边界条件不明 | 开发前补充 |
| 🟢 **一般** | 提示类问题、建议优化、非阻塞项 | 可后续迭代 |

> ⚠️ **杜绝提出"一般"级别的运维细节问题！**

---

### 步骤 4：生成评审结论

**问题严重级别定义：**

| 级别 | 定义 | 处理方式 |
|------|------|----------|
| 🔴 **严重** | 阻塞开发/上线，必须解决后才能继续 | 评审会议必须讨论 |
| 🟡 **重要** | 影响设计实现，建议评审会议讨论 | 开发前补充 |
| 🟢 **一般** | 优化建议，可后续完善 | 可后续迭代 |

---

### 步骤 5：生成评审 JSON 数据文件 ⭐

> ⚠️ **AI 分析完成后，必须先生成 JSON 数据文件，不可直接 write_file 生成 MD 报告！**

**5.1 JSON 数据结构**

按照本文档「JSON 格式规范」章节定义的结构组织评审数据。

**5.2 JSON 文件命名**

```
2.{需求名}_评审数据.json
```

**5.3 JSON 文件路径**

输出到被评审 MD 文件所在目录。

**5.4 JSON 必填字段检查**

| 字段路径 | 必填 | 说明 |
|---------|------|------|
| `doc_title` | ✅ | 文档名称 |
| `doc_type` | ✅ | 需求文档/技术方案/混合文档 |
| `review_date` | ✅ | YYYY-MM-DD 格式 |
| `reviewer` | ✅ | 固定值：AI 评审 |
| `summary.total_issues` | ✅ | 问题总数 |
| `summary.critical/major/minor` | ✅ | 各级别数量 |
| `categories[].name` | ✅ | 分类名称 |
| `categories[].severity` | ✅ | critical/major/minor |
| `categories[].items[]` | ✅ | 问题项数组 |
| `items[].location` | ✅ | 问题位置 |
| `items[].description` | ✅ | 问题描述 |
| `items[].suggestion` | ✅ | 改进建议 |

**5.5 特殊字段（8 个必选状态标记）**

| 字段路径 | 对应状态标记 |
|---------|-------------|
| `document_status.knowledge_base` | 知识库读取状态 |
| `document_status.related_documents` | 关联文档读取状态 |
| `document_status.images` | 图片解析状态 |
| `document_status.attachments` | 附件解析状态 |
| `comparison.diff_items` | 需求-技术方案对比分析 |
| `historical_comparison.items` | 历史对比分析 |
| `tech_risks` | 技术风险提示 |
| `items[].status` | 问题处理状态标记 |

---

### 步骤 6：调用脚本生成 MD 报告 ⭐

> ⚠️ **必须调用脚本生成 MD 报告，不可手动 write_file！**

**6.1 调用命令**

```bash
python {技能目录}/scripts/generate_review_report.py --input <评审JSON路径> --output <user_story_dir>/2.<需求名>_评审报告.md
```

**6.2 参数说明**

| 参数 | 说明 | 示例 |
|------|------|------|
| `--input` | 评审 JSON 文件路径 | `E:\项目\2.需求_评审数据.json` |
| `--output` | 输出 MD 报告路径 | `E:\项目\2.需求_评审报告.md` |

**6.3 输出验证**

脚本执行后，验证以下 8 个必选状态标记是否包含在报告中：

| 序号 | 状态标记 | 验证方式 |
|------|---------|---------|
| 1 | 知识库读取状态 | 检查「知识库读取状态」章节 |
| 2 | 关联文档读取状态 | 检查「关联文档读取状态」章节 |
| 3 | 图片解析状态 | 检查「图片解析状态」章节 |
| 4 | 附件解析状态 | 检查「附件解析状态」章节（如有附件） |
| 5 | 需求-技术方案对比分析 | 检查对比分析章节（如有技术方案） |
| 6 | 历史对比分析 | 检查历史对比章节（如有历史记录） |
| 7 | 技术风险提示 | 检查技术风险章节 |
| 8 | 问题处理状态标记 | 检查每个问题是否包含「处理状态」 |

**6.4 流程总结**

```
AI 多维度分析 → 生成评审 JSON 数据 → 调用脚本 → 输出 MD 评审报告
      │                │                  │                │
   (步骤1-4)        (步骤5)           (步骤6)          (输出验证)
```

---

## 评审报告格式

```markdown
# 📋 需求与技术方案评审报告

> 文档名称：{文档名}
> 文档类型：{类型}
> 评审日期：{日期}
> 评审人：AI 评审助手

---

## 📚 知识库读取状态

| 知识库内容 | 文档名称 | 读取状态 | 提取内容 |
|-----------|---------|---------|---------|

---

## 📊 评审概览

| 指标 | 数量 |
|------|------|
| 问题总数 | N |
| 🔴 严重 | N |
| 🟡 重要 | N |
| 🟢 一般 | N |

---

## 🔴 严重问题

### 1. [维度] 问题标题

- **位置**：章节号
- **描述**：问题描述
- **建议**：改进建议

---

## 🟡 重要问题

...

---

## 🟢 一般问题

...

---

## ✅ 完整性检查结果

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 版本记录 | ✅ 有 | V1.0、V1.1 |
| 需求背景 | ✅ 有 | 已说明 |
| 接口定义 | ❌ 缺失 | 缺少内部接口定义 |
| ... | ... | ... |

---

## 💡 总结建议

### 高优先级（评审会议必须讨论）

1. xxx

### 中优先级（开发前补充）

1. xxx

### 低优先级（可后续完善）

1. xxx

---

*本报告由 AI 评审助手生成，需人工审核确认*
```

---

## 配置信息

AI 执行时使用以下配置：

| 配置项 | 变量 | 来源 |
|--------|------|------|
| 知识库路径 | `{kb_path}` | config/project.yaml |
| 项目组 | `{{team}}` | config/project.yaml |
| 产品 | `{{product}}` | config/project.yaml |

---

## 使用示例

### 基础用法（单文件评审）

```
用户：帮我评审 E:\项目\额度测算.md 这个需求文档

AI 执行：
1. 读取需求文档
2. 自动识别文档类型
3. 搜索知识库历史文档
4. 搜索关联技术方案（如有）
5. 解析图片（如有）
6. 按对应维度评审
7. 生成评审报告
```

### 技术方案评审

```
用户：帮我评审 E:\项目\TR_额度测算.md 这个技术方案

AI 执行：
1. 读取技术方案文档
2. 搜索关联需求文档
3. 搜索需求评审报告
4. 对比分析需求-技术方案
5. 按技术方案维度评审
6. 生成评审报告
```

### 带历史对比

```
用户：用 E:\项目\额度测算.md 和 E:\项目\历史\额度测算_v1.0.md 对比评审

AI 执行：
1. 读取两个文档
2. 对比分析差异
3. 识别与历史逻辑冲突点
4. 生成评审报告
```

---

## 注意事项

1. **评审结果需人工确认**：AI 评审作为参考，需产品/架构师确认
2. **技术方案必须关联需求**：不可独立评审
3. **图片必须解析**：流程图、状态图是关键信息
4. **历史对比必须执行**：知识库历史文档不可跳过
5. **输出位置**：评审报告输出到被评审 MD 文件所在目录
6. **严重级别定义**：阻塞=严重、影响=重要、优化=一般

---

*版本：v1.1*
*更新日期：2026-07-24*
*更新：步骤②产出文件统一添加 "2." 前缀，便于在工作目录中直观识别由哪一步生成*
