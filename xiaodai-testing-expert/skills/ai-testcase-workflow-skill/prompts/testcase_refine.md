# 测试用例细化 Prompt

> 此 Prompt 用于 AI 将评审后的 XMind 测试点扩展为详细的EXCEL测试用例，导入DMP

---

## ⛔ 禁止 AI 擅自做决定（强制按流程规范执行）

> **⚠️ 执行此 Prompt 前，必须完整阅读本文档！禁止凭记忆或推断执行！**

| 禁止项 | 说明 | 正确做法 |
|--------|------|---------|
| ⛔ 禁止假设已读取 | 不假设前置步骤已完成，必须实际执行每步读取操作 | 每个步骤按规范重新读取，不假设前置状态 |
| ⛔ 禁止简化 Todo | Todo 清单必须完整，不可省略强制检查项（⭐标记项） | 按规范创建完整 Todo 清单（10项），逐项标记状态 |
| ⛔ 禁止跳过步骤 | 强制项（⭐）不可跳过，必须逐项执行并标记状态 | 强制项必须执行，AI 无权判断跳过 |
| ⛔ 禁止自行判断 | AI 不可自行判断「不重要」「已完成」「无需执行」 | 按流程规范执行，不做任何擅自决策 |
| ⛔ 禁止假设状态 | 标记「已完成」前必须实际执行该操作，不可凭记忆标记 | 每项必须实际执行后才能标记 completed |
| ⛔ 禁止跳过 XMind 解析 | 调用脚本解析评审后 XMind 不可跳过 | 必须调用 parse_xmind.py 解析 XMind |
| ⛔ 禁止跳过整理版 MD 读取 | 如存在整理版 MD 必须读取 | 必须读取以补充业务上下文 |
| ⛔ 禁止跳过技术方案读取 | 如存在技术方案必须读取 | 必须读取以补充接口定义 |

**典型违规场景**：

| 违规行为 | 正确做法 |
|---------|---------|
| 假设"XMind已解析，直接使用JSON" | 步骤⑥必须调用脚本解析 XMind，获取唯一测试点来源 |
| 简化 Todo 为 5 项而非 10 项 | 按规范创建完整 Todo 清单，逐项标记状态 |
| 标记"已完成"但实际未执行读取操作 | 每项必须实际执行后才能标记 completed |
| 整理版 MD 存在但不读取 | 必须读取整理版 MD 补充业务规则、边界条件 |
| 技术方案存在但不读取 | 必须读取技术方案补充接口定义、字段说明 |

---

## ⚠️ 执行前必须创建 Todo 清单 ⭐

> **AI 执行此流程时，必须先调用 `todo_write` 创建以下 todo 清单，逐项完成并标记状态！**

**步骤⑥子 Todo 清单（9项强制步骤）**：

| 序号 | 步骤 | 强制 | 输出/验证 |
|------|------|------|----------|
| 1 | 读取 JSON 格式规范（本文档「JSON 格式规范」章节） | ⭐ 强制 | 理解 testcases[].id、preCondition、input、output 等关键字段 |
| 2 | 调用脚本解析评审后的 XMind | ⭐ **强制** | **唯一测试点来源！调用 parse_xmind.py 解析。文件名不限，检查内容是否包含评审标记** |
| 3 | 读取项目配置提取 team/product/modulePath | ⭐ 强制 | 从 {项目根}/.skill/project.yaml 读取，**不需要用户输入** |
| 4 | 用户问答收集参数 | ⭐ 强制 | caseGroup/version/manager/relateReqCode（只需收集 4 个参数） |
| 5 | 读取整理版 MD（如存在） |  强制（如存在） | **一份文件包含所有内容**：需求文档 + 技术方案 + 图片解析 + 附件 |
| 6 | 测试点细化为测试用例 |  强制 | 每个测试点生成完整步骤（1、2、格式） |
| 7 | 按格式规范生成 JSON | ⭐ 强制 | 逐字段对照验证，调用 refine_testcases.py |
| 8 | 调用脚本生成 Excel | 必选 | 调用 generate_excel.py，验证脚本输出成功 |
| 9 | 呈现用例统计和输出文件 | ⭐ 强制 | 展示生成结果 |

**AI 执行流程**：
1. 收到步骤⑥指令 → 立即调用 `todo_write` 创建 9 项子清单
2. 逐项执行 → 每完成一项立即标记 `completed`
3. 强制项跳过 → 用户会看到 todo 状态未完成 → 立即发现问题

**⚠️ 参数来源说明**：
- team/product/modulePath → 从 `{项目根}/.skill/project.yaml` 读取，**不需要用户输入**
- caseGroup/version/manager/relateReqCode → 用户问答收集

---

## 任务目标

将**评审后**的 XMind 测试点扩展为详细测试用例，输出 Excel 格式用于测试平台导入。AI 自动将测试点细化为具体步骤，生成测试数据，确保用例可执行、可验证。

---

## 输入

| 输入项 | 来源 | 必选 | 说明 |
|--------|------|------|------|
| **评审后的 XMind** | 用户提供（直接在 AI 生成的原始 XMind 上添加颜色标记，无需重命名） | ⭐ **必选** | **唯一测试点来源**，调用脚本解析为 JSON |
| 测试用例参数 | 用户问答收集 | ⭐ 必选 | caseGroup/version/manager/relateReqCode |
| 项目配置 | {项目根}/.skill/project.yaml | ⭐ **必选** | team/product/modulePath 从配置读取 |
| 整理版 MD | 同目录 `{需求名}_整理版*.md` | ⭐ 如存在 | 补充业务规则、边界条件、业务上下文 |
| 技术方案 | 同目录 `TR_*.md` | 推荐 如存在 | 补充接口定义、字段说明、数据结构 |

> 💡 **整理版 MD 存在时必须读取**，结合 MD 分析推导用例；不存在时仅凭 XMind 细化

---

## 输出

> ⚠️ **输出路径规则**：所有输出文件统一存放到用户故事工作目录（`D:\效贷-产品需求\{用户故事编号}-{用户故事名称}`），由步骤①确认并缓存为 `{user_story_dir}`。如会话中已缓存用户故事目录路径，直接使用；如未缓存，需先向测试人员确认用户故事信息（编号+名称）并定位/创建工作目录。

| 输出项 | 格式 | 命名 | 输出路径 | 说明 |
|--------|------|------|---------|------|
| 测试用例 Excel | .xlsx | `6.{需求名}_测试用例.xlsx` | `{user_story_dir}/` | ⭐ 必选 | 步骤⑥产出，文件名前缀 "6." |
| 测试用例 JSON | .json | `6.{需求名}_测试用例.json` | `{user_story_dir}/` | ⭐ 必选 | 步骤⑥产出，文件名前缀 "6." |
| 评审后快照 JSON | .json | `6.{需求名}_测试点_reviewed.json` | `{user_story_dir}/` | 模式A（完整流程）时生成，文件名前缀 "6." |

> 💡 **模式A（完整流程）**：需要生成 `6.*_测试点_reviewed.json`，步骤⑦入库时对比 `4.*_测试点.json`（AI 原始）vs `6.*_测试点_reviewed.json`（评审后），计算 AI 遗漏率/通过率
> 💡 **模式B（单步模式）**：无需差异对比，只生成用例 JSON + Excel 即可

---

## JSON 格式规范 ⭐

```json
{
  "metadata": {
    "title": "{需求名称}_测试用例",
    "source_xmind": "4.{需求名称}_测试点.xmind",
    "generated_date": "2026-05-06",
    "generated_by": "AI智能细化",
    "statistics": {
      "total_cases": 50,
      "functional": 35,
      "integration": 10,
      "security": 3,
      "performance": 2
    },
    "user_config": {
      "team": "{{team}}",
      "product": "{{product}}",
      "modulePath": "{{modulePath}}",
      "caseGroup": "{用户提供的功能路径}",
      "version": "{用户提供的版本号}",
      "manager": "{用户提供的责任人}",
      "relateReqCode": "{用户提供的需求编码}"
    }
  },
  "testcases": [
    {
      "id": "TC-FUNC-001",
      "team": "{{team}}",
      "caseGroup": "{功能路径}",
      "name": "列表数据状态展示校验",
      "preCondition": "登录系统，进入数据管理页面，存在一条状态为\"已提交\"的数据记录",
      "input": "1、进入数据管理页面\n2、查看数据列表的状态列",
      "output": "1、成功进入页面，数据列表正常加载\n2、状态列显示\"已提交\"",
      "product": "{{product}}",
      "modulePath": "{{modulePath}}",
      "version": "{版本号}",
      "caseType": "功能测试",
      "source": "需求文档",
      "caseLevel": "P1",
      "manager": "{责任人}",
      "autoState": "否",
      "relateReqCode": "{需求编码}",
      "workload": "",
      "remarks": "验证状态字段展示正确",
      "separator": "\n",
      "marker_type": "确认",
      "original_path": "需求文档测试点/列表展示/功能"
    }
  ]
}
```

**字段说明**：

| 字段路径 | 含义 | 必填 | 说明 |
|---------|------|------|------|
| `metadata.title` | 测试用例名称 | ✅ | 格式：`{需求名称}_测试用例` |
| `metadata.source_xmind` | 来源 XMind 文件名 | ✅ | 格式：`4.{需求名称}_测试点.xmind`（步骤④生成的 XMind，前缀 "4."） |
| `metadata.generated_date` | 生成日期 | ✅ | YYYY-MM-DD 格式 |
| `metadata.statistics` | 用例统计 | ✅ | 总数 + 分类统计 |
| `metadata.user_config` | 用户参数 | ✅ | 用户问答收集的参数 |
| `testcases` | 测试用例数组 | ✅ | 每个元素为一条用例 |
| `testcases[].id` | 用例编号 | ✅ | 格式：`TC-{类型}-{序号}` |
| `testcases[].team` | 项目组 | ✅ | 从配置读取 |
| `testcases[].caseGroup` | 功能路径 | ✅ | 用户问答收集 |
| `testcases[].name` | 用例名称 | ✅ | 描述性名称 |
| `testcases[].preCondition` | 前置条件 | ⚠️ | 包含登录状态、数据状态等 |
| `testcases[].input` | 步骤 | ✅ | 使用 `1、` 格式，换行分隔 |
| `testcases[].output` | 预期结果 | ✅ | 与步骤数量对应 |
| `testcases[].caseType` | 用例类型 | ✅ | 功能测试/集成测试/安全漏洞/性能测试 |
| `testcases[].caseLevel` | 用例级别 | ⚠️ | P0/P1/P2 |
| `testcases[].marker_type` | 评审标记 | ✅ | 确认/新增/修改/删除 |
| `testcases[].original_path` | 来源路径 | ✅ | XMind 中的层级路径 |

---

## 脚本调用规范 ⭐

> ⚠️ **执行脚本前必须先查看参数定义，避免参数格式错误！**

### 参数查看方式

```bash
python scripts/<脚本名>.py -h
```

### 各脚本参数定义

| 脚本 | 位置参数 | 选项参数 | 正确命令示例 |
|------|---------|---------|-------------|
| parse_xmind.py | `input` (XMind文件) | `-o OUTPUT` | `python parse_xmind.py "xxx.xmind" -o "xxx.json"` |
| refine_testcases.py | `input` (测试点JSON) | `-o OUTPUT`, `--caseGroup`, `--version`, `--manager`, `--relateReqCode`, `--team`, `--product`, `--modulePath` | `python refine_testcases.py "xxx.json" --caseGroup "xxx" --version "xxx" --manager "xxx"` |
| generate_excel.py | `input` (测试用例JSON) | `-o OUTPUT`, `--caseGroup`(必选), `--version`(必选), `--manager`(必选), `--relateReqCode`, `--team`, `--product`, `--modulePath`, `--project-root` | `python generate_excel.py "xxx.json" --caseGroup "xxx" --version "xxx" --manager "xxx"` |

**⚠️ 注意事项**：
- `input` 是**位置参数**，不需要 `--input` 标志
- `-o` 是短选项，也可以用 `--output`
- `--caseGroup/--version/--manager` 在 generate_excel.py 中是**必选参数**

---

## 执行步骤

### 步骤 1：解析评审后的 XMind 并调用脚本转化为 JSON ⭐

> ⚠️ **这是步骤⑥的唯一数据来源！必须调用脚本转化！**

**执行逻辑**：

```
1. 搜索 XMind 文件（{需求名}_测试点.xmind，用户已在此文件上添加评审标记）
2. 调用脚本解析 XMind 并转化为 JSON：
   python scripts/parse_xmind.py "4.{需求名}_测试点.xmind" -o "6.{需求名}_测试点_reviewed.json"
   ⚠️ 注意：input 是位置参数，不需要 --input 标志！
3. 脚本解析内容：
   - 提取测试点层级结构（modules → sub_modules → testpoints）
   - 提取评审标记（marker_type：确认/新增/修改/删除）
   - 提取来源标识（source：requirement/review/tech/knowledge_base）
4. 统计评审标记分布
5. 过滤删除标记的测试点（marker_type=删除 → 不生成用例）
```

**呈现解析状态**：

```markdown
## 📄 评审后 XMind 解析状态

| 项目 | 数量 | 说明 |
|------|------|------|
| 总测试点 | N | 解析后的总数 |
| ✅ 确认 | N | 用户确认的测试点 → 生成用例 |
| 🔵 新增 | N | 用户新增的测试点 → 生成用例 |
| 🟠 修改 | N | 用户修改的测试点 → 生成用例 |
| ⚫ 删除 | N | 用户删除的测试点 → **不生成用例** |

> ⚠️ 删除标记的测试点不生成测试用例！
```

**输出文件**：`6.{需求名}_测试点_reviewed.json`

---

### 步骤 2：收集用户参数 ⭐

> ⚠️ **必须向用户收集以下参数，用于生成测试用例！**
>
> **注意**：team、product、modulePath 从 project.yaml 配置文件自动读取，不需要用户输入！

**参数来源说明**：

| 参数 | 来源 | 说明 |
|------|------|------|
| team | project.yaml defaults.team | ⚠️ **从配置读取，不需要用户输入** |
| product | project.yaml defaults.product | ⚠️ **从配置读取，不需要用户输入** |
| modulePath | project.yaml defaults.modulePath | ⚠️ **从配置读取，不需要用户输入** |
| caseGroup | 用户问答收集 | ⭐ 必须向用户收集 |
| version | 用户问答收集 | ⭐ 必须向用户收集 |
| manager | 用户问答收集 | ⭐ 必须向用户收集 |
| relateReqCode | 用户问答收集 | ⭐ 必须向用户收集 |

**问答模板（只需收集 4 个参数）**：

```
📋 请提供以下测试用例参数：

1. 功能路径（caseGroup）格式：年份-月份-日期-需求名称
   示例：2026-202604-20260420-额度测算

2. 版本号（version）格式：VYYYY.MM.DD
   示例：V2026.04.16

3. 责任人（manager）
   请填写姓名（重名时加工号）

4. 需求编码（relateReqCode）
   示例：PRJ-00737131 
```

**读取配置文件**：

```
读取 {项目目录}/.skill/project.yaml，提取：
- defaults.team
- defaults.product  
- defaults.modulePath
```

---

### 步骤 3：读取整理版 MD 和技术方案（补充业务上下文） ⭐

> ⚠️ **如存在整理版 MD 必须读取！**
>
> XMind 提供测试点结构，整理版 MD 提供业务规则、接口定义等细化信息。

**读取目的**：

| 文档 | 提供内容 | 用于细化 |
|------|---------|---------|
| 整理版 MD | 业务规则、边界条件、状态定义、字段说明、图片解析结果 | 前置条件、预期结果、测试数据 |
| 技术方案 MD（如有） | 接口参数、请求/响应结构、字段类型、数据库字段 | 输入数据、预期结果验证 |

**读取策略**：

```
1. 搜索整理版 MD：同目录搜索 *整理版*.md
   - 存在 → 必须读取，提取业务规则、边界值、状态流转、字段定义，结合 MD 分析推导用例
   - 不存在 → 仅凭 XMind 细化
2. 搜索技术方案：TR_*.md 或包含"技术方案"关键词的文档
   - 存在 → 读取，提取接口URL、参数名、字段类型、响应结构
   - 不存在 → 跳过，不影响用例生成
```

**呈现读取状态**：

```markdown
## 📄 文档读取状态

| 文档 | 状态 | 提取的关键信息 |
|------|------|---------------|
| 整理版 MD | ✅ 已读取 | 业务规则：XXX；边界值：XXX；状态定义：XXX |
| 技术方案.md | ✅ 已读取 | 接口：XXX；参数：XXX；响应字段：XXX |

或（整理版 MD 不存在时）：

| 文档 | 状态 | 说明 |
|------|------|------|
| 整理版 MD | ❌ 不存在 | 仅凭 XMind 细化用例 |
| 技术方案.md | ❌ 不存在 | 跳过 |
```

---

## 评审标记处理规则 ⭐

> ⚠️ **XMind 评审标记必须正确解析和处理！**

| 颜色 | 含义 | 处理方式 |
|------|------|----------|
| 🔵 蓝色 | ➕ 新增 | 生成对应用例 |
| 🟠 橙色 | 🔄 修改 | 按调整内容生成用例 |
| ⚫ 灰色 | ❌ 删除 | 不生成用例 |
| 无标记 | ✅ 确认 | 正常生成用例 |

---

## 测试用例分类 ⭐

| 分类 | caseType | 侧重点 | 占比建议 |
|------|----------|--------|----------|
| 功能测试 | 功能测试 | 单模块业务、界面交互 | 60%~70% |
| 集成测试 | 集成测试 | 跨模块联调、端到端流程 | 20%~30% |
| 安全测试 | 安全漏洞 | 数据安全、权限控制 | 5%~10% |
| 性能测试 | 性能测试 | 响应时间、并发处理 | 按需 |

**判定规则：**

| 场景 | 归入分类 |
|------|----------|
| 单模块字段校验、按钮操作、页面展示 | 功能测试 |
| 涉及两个及以上模块/接口的流程 | 集成测试 |
| 涉及数据加密、权限校验、敏感数据 | 安全测试 |
| 涉及响应时间、并发、大数据量 | 性能测试 |

---

## 用例细化规则 ⭐

### 步骤编号规范

**必须使用「1、」「2、」格式，禁止使用「1. 」格式！**

```
✅ 正确：
1、进入页面
2、查看字段
3、验证展示

❌ 错误：
1. 进入页面    ← 禁止使用点+空格
2. 查看字段
```

### 步骤数量要求

**每个用例步骤数量 >= 2 个！**

如原有步骤只有 1 个，需补充前置操作步骤。

### 步骤与预期结果对应 ⭐

**步骤数量 = 预期结果数量，必须一一对应！**

```
✅ 正确：
步骤：
  1、点击「XXXX功能」按钮
  2、进入协议阅读页

预期结果：
  1、按钮点击成功，发起XX请求
  2、成功进入协议阅读页

❌ 错误：步骤=2，预期结果=1，不匹配
```

### 数据实例化 ⭐

**必须使用具体测试数据，禁止抽象占位符！**

| 反例 | 正例 |
|------|------|
| 「状态为某值」 | 「状态=已上传」 |
| 「查看字段值」 | 「查看税务数据状态字段，值为"已上传"」 |
| 「输入金额」 | 「输入贷款金额=500000」 |
| 「选择客户」 | 「选择客户编码=C001」 |

---

## Excel 格式规范 ⭐

**前 4 行为固定模板行：**

| 行号 | 内容 |
|------|------|
| 第1行 | `用例管理 # dmp_testcase` |
| 第2行 | 说明行（必填字段提示） |
| 第3行 | 列名行（team, caseGroup, name, ...） |
| 第4行 | 字段说明行（*项目组, *功能路径, ...） |
| 第5行起 | 测试用例数据 |

**列定义：**

| 列 | 列名 | 必填 | 说明 |
|----|------|------|------|
| A | team | ✅ | 项目组 |
| B | caseGroup | ✅ | 功能路径 |
| C | name | ✅ | 用例名称 |
| D | preCondition | | 前置条件 |
| E | input | | 步骤（换行分隔） |
| F | output | | 预期结果（换行分隔） |
| G | product | ✅ | 产品 |
| H | modulePath | ✅ | 模块路径 |
| I | version | | 适用版本 |
| J | caseType | ✅ | 用例类型 |
| K | source | | 来源 |
| L | caseLevel | | 用例级别（P0/P1/P2） |
| M | manager | ✅ | 责任人 |
| N | autoState | | 自动化状态 |
| O | relateReqCode | | 关联用户故事 |
| P | workload | | 工作量 |
| Q | remarks | | 备注 |
| R | separator | | 分隔符 |

---

## 配置信息

AI 执行时使用以下配置：

| 配置项 | 变量 | 来源 |
|--------|------|------|
| 项目组 | `{{team}}` | {项目根}/.skill/project.yaml |
| 产品 | `{{product}}` | {项目根}/.skill/project.yaml |
| 模块路径 | `{{modulePath}}` | {项目根}/.skill/project.yaml |

---

## 执行命令

**AI 按步骤调用各脚本：**

```bash
# 步骤1：解析评审后 XMind（输入为步骤④生成的 4.<需求名>_测试点.xmind）
python scripts/parse_xmind.py "4.<需求名>_测试点.xmind" -o "6.<需求名>_测试点_reviewed.json"

# 步骤2：细化测试点为用例（输出 6.<需求名>_测试用例.json）
python scripts/refine_testcases.py "6.<需求名>_测试点_reviewed.json" \
    -o "6.<需求名>_测试用例.json" \
    --caseGroup "<功能路径>" \
    --version "<版本号>" \
    --manager "<责任人>" \
    --relateReqCode "<需求编码>"

# 步骤3：生成 Excel（输出 6.<需求名>_测试用例.xlsx）
python scripts/generate_excel.py "6.<需求名>_测试用例.json" \
    -o "6.<需求名>_测试用例.xlsx" \
    --caseGroup "<功能路径>" \
    --version "<版本号>" \
    --manager "<责任人>" \
    --relateReqCode "<需求编码>"
```

---

## 注意事项

1. **输入是用户评审后的 XMind**：直接在 AI 生成的原始 XMind 上添加颜色标记，无需重命名
2. **评审标记必须解析**：正确处理新增/修改/删除标记
3. **数据必须实例化**：步骤、前置条件、预期结果使用具体值
4. **步骤编号规范**：使用「1、」格式，换行分隔
5. **步骤数量 >= 2**：每个用例至少 2 个步骤
6. **步骤与预期结果对应**：数量相等，一一对应
7. **分类占比合理**：集成测试占比 20%~40%
8. **P0 数量限制**：一个需求主流程不超过 3 个 P0
9. **前置条件完整**：包含登录状态、数据状态、配置信息
10. **预期结果可验证**：描述具体状态、值、提示信息，避免模糊表述

---

## 用例细化示例

### 示例1：功能测试用例

**输入测试点**：列表状态字段展示校验

**细化输出**：
```
用例名称：列表状态字段展示校验
前置条件：登录系统，进入数据管理页面，存在一条状态为"已提交"的数据记录
步骤：
  1、进入数据管理页面
  2、查看数据列表的状态列
预期结果：
  1、成功进入页面，数据列表正常加载
  2、状态列显示"已提交"
用例类型：功能测试
用例级别：P1
```

### 示例2：集成测试用例

**输入测试点**：外部接口调用失败处理

**细化输出**：
```
用例名称：外部接口调用失败自动重试
前置条件：
  - 登录系统
  - 外部接口配置超时时间=30秒
  - 重试次数配置=3次
步骤：
  1、点击「提交数据」按钮
  2、系统调用外部接口
  3、模拟接口返回失败（状态码=500）
  4、等待系统自动重试
预期结果：
  1、提交请求发起成功
  2、系统检测到接口失败
  3、自动重试第1次（等待30秒）
  4、重试3次仍失败后，状态更新为"提交失败"，告警通知触发
用例类型：集成测试
用例级别：P0
```

### 示例3：安全测试用例

**输入测试点**：敏感数据加密存储验证

**细化输出**：
```
用例名称：敏感字段加密存储验证
前置条件：
  - 登录系统
  - 用户手机号=13800138000
  - 数据库查询权限已开通
步骤：
  1、提交用户信息表单
  2、查询数据库用户表
  3、查看手机号字段值
预期结果：
  1、提交成功，数据已保存
  2、数据库记录存在
  3、手机号字段值为加密后的密文（非原始手机号）
用例类型：安全漏洞
用例级别：P1
```

### 示例4：表单提交用例

**输入测试点**：必填字段为空提交校验

**细化输出**：
```
用例名称：必填字段为空提交校验
前置条件：登录系统，进入表单提交页面
步骤：
  1、进入表单提交页面
  2、填写非必填字段（描述=测试备注）
  3、不填写必填字段（名称=空）
  4、点击「提交」按钮
预期结果：
  1、成功进入页面
  2、非必填字段填写成功
  3、必填字段保持空白
  4、提交失败，提示"名称为必填项，请填写"
用例类型：功能测试
用例级别：P1
```

---

*版本：v1.1*
*更新日期：2026-07-24*
*更新：步骤⑥产出文件统一添加 "6." 前缀；来源 XMind 统一使用步骤④的 "4." 前缀，便于在工作目录中直观识别文件归属步骤*